# Release delta: v0.112-0 → v0.113-0

Derived from source code at tags `v0.112-0` (2b07655542) and `v0.113-0` (c0cdc8c261). Independent of CHANGELOG.md.

## Configuration (GUCs)

### Default changes (2)

- `documentdb_rum.enable_new_bulk_delete`: `false` → `true`
- `documentdb_rum.enable_new_bulk_delete_inline_data_pages`: `true` → `false`

### Added (17)

- `documentdb.enableCompositeReducedCorrelatedPrefixTrim` (bool, default `true`) — Whether to enable prefix-group-aware trimming of secondary variable bounds for reduced correlated composite indexes.
- `documentdb.enableDeadIndexEntryMarkingByTTLTask` (bool, default `false`) — Whether to enable marking dead index entries during TTL task scans to avoid redundant heap fetches.
- `documentdb.enableDuplicateFieldFix` (bool, default `true`) — Whether to enable fix for duplicate fields in addToSet.
- `documentdb.enableDynamicCursors` (bool, default `false`) — Whether or not to enable dynamic cursors for aggregation query rewrites.
- `documentdb.enableFailureOnParallelIndexArrays` (bool, default `false`) — Whether to fail when parallel arrays are indexed in composite indexes.
- `documentdb.enableIndexOnlyScanForCoveredAggregateTargets` (bool, default `true`) — Whether to enable index only scan for aggregate target-list expressions that reference covered document paths.
- `documentdb.enableIndexOnlyScanForFindProject` (bool, default `false`) — Whether or not to enable index only scan for find with project operations. Test GUC for now since we need to support this with cursors properly.
- `documentdb.enableIndexOnlyScanForRangeMatch` (bool, default `true`) — Whether to enable index only scan for range-match qualifiers on covered index paths.
- `documentdb.enableLocalRetryTable` (bool, default `true`) — Whether to use a single local retry table instead of per-collection distributed retry tables (After retirement move it to testing configs)
- `documentdb.enableNewNamespaceValidation` (bool, default `false`) — Whether to enable new namespace validation.
- `documentdb.enablePlannerStatisticsNewCollections` (bool, default `false`) — Whether to enable custom planner statistics for any new collections.
- `documentdb.enableSkipDottedFieldIndexTerms` (bool, default `true`) — Whether to skip generating index terms for fields with dotted names (e.g. literal "a.b" field).
- `documentdb.enableSortPushToAccumulator` (bool, default `false`) — Whether to push sort order into accumulator in $sortGroup.
- `documentdb.enableUniqueReindex` (bool, default `false`) — Whether to enable unique reindex.
- `documentdb.indexBuildsScheduledOnBgWorker` (bool, default `false`) — Whether to schedule index builds via background worker jobs.
- `documentdb.skipIndexCleanupOnFailure` (bool, default `false`) — Whether or not to skip index cleanup on failure.
- `documentdb.skipIndexCleanupOnReindex` (bool, default `false`) — Whether or not to skip index cleanup on reindex.

### Removed (4)

- `documentdb.enableIdIndexPushdown`
- `documentdb.enableRumNewCompositeTermGeneration`
- `documentdb.enableUpdateBsonDocument`
- `documentdb.lowSelectivityForLookup`

## SQL functions (from *--latest.sql UDF scripts)

### Added (3)

- `__API_CATALOG_SCHEMA__.dollar_support_object_id`
- `__API_SCHEMA_INTERNAL_V2__.cursor_tracker`
- `documentdb_api_internal_readonly.cursor_tracker`

### Removed (1)

- `__API_SCHEMA_INTERNAL__.bson_update_document`
